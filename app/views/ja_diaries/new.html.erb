<h1>日本語で日記を書きましょう！</h1>

<%= form_with(model: @ja_diary, url: new_en_diary_path, method: :get, local: true) do |form| %>
  <div class="ja_form">
    <%= form.text_area :body, class: "ja_text_area" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>