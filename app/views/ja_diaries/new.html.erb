<h1>日本語で日記を書きましょう！</h1>

<%= form_with(model: @ja_diary, url: new_en_diary_path, method: :get, local: true) do |form| %>
  <div>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>